export function routePaths() {
  const paths = [
    "/login",
    "/register",
    "/activate-email",
    "/register-completed",
    "/forgot-password",
    "/reset-password/",
  ];
  return paths;
}
